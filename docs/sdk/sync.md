# Legacy `SfmApiClient` (sync)

This is the deprecated hand-written Python client. New consumers should
use `sceneapi_client_gen.Client` from the generated SDK described in
{doc}`index`.

This page is retained only as source history and is excluded from the
public documentation build. The implementation now lives in the
`sfmapi-sdk` repository.
