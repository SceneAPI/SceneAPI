# Architecture Decision Register

Single-page index of every architectural decision in sfmapi: what's
locked, what's cancelled (and why), what's proposed but waiting on
user input. Cross-links the long-form sources.

When a decision changes from one status to another, update this
register first — it's the single place a new contributor or returning
user reads to understand "what is settled here?"

---

## Locked decisions

These are committed and load-bearing. Don't reopen without an
explicit decision-record update.

| ID | Decision | Source | Why |
|---|---|---|---|
| L1 | One GPU per instance; per-GPU concurrency for SfM tasks = 1. Scale = more instances. | `CLAUDE.md` §"Locked Constraints" | CUDA + heavy memory model; concurrency at process level, not within-process |
| L2 | Multi-tenant from day 1. `tenant_id NOT NULL DEFAULT 'default'` everywhere. `current_tenant()` returns `'default'` until auth lands. | `CLAUDE.md` §"Locked Constraints" | Cheap to add now, expensive to retrofit |
| L3 | Image inputs: `upload \| local \| s3` behind one `ImageSource` abstraction. Local 50GB dirs MUST NOT be copied. | `CLAUDE.md` §"Locked Constraints" | Avoid disk-bound bottleneck on local-source workflows |
| L4 | Sealed-snapshot progress only. API NEVER reads live `database.db` or live `sparse/`. | `CLAUDE.md` §"Locked Constraints" | Race-free reads; entire foundation of "API never sees half-written state" |
| L5 | DAG: in-house orchestrator + ARQ as per-task executor. One Task = one ARQ job. | `CLAUDE.md` §"Locked Tech Decisions" | DAG semantics are sfmapi's; ARQ is just a scheduler |
| L6 | DB: SQLite for v0, Postgres-compatible. ANSI SQL only. CI runs both. | `CLAUDE.md` §"Locked Tech Decisions" | SQLite-as-prod is a real deployment target (single-user / on-prem / embedded) |
| L7 | Upload: roll-our-own chunked. `POST /uploads → upload_id`, `PATCH` with `Content-Range`, `POST /finalize`. `Idempotency-Key` from day 1. | `CLAUDE.md` §"Locked Tech Decisions" | TUS-resumable was overkill; this is the minimum for our use case |
| L8 | Points binary: 26 B/point + 44 B header. `application/x-sfm-points-v1`. Cursor pagination via HTTP `Range`. | `CLAUDE.md` §"Locked Tech Decisions" + `app/schemas/points_binary.py` | 80% size reduction vs JSON; cache-friendly; standard HTTP semantics |
| L9 | ~~Realtime: SSE-only for v0. Reserve `/ws/v1/...` for later.~~ **Superseded by L37** — the WebSocket at `/ws/v1/jobs/{job_id}` is fully shipped, wired (`app/main.py` includes `ws_jobs.router`), and normatively specified in `SFMAPI-SPEC.md` §8. The "reserve `/ws/v1/...`" rationale is satisfied: the prefix was held, then used for exactly the anticipated surface. SSE remains the primary progress feed. | `docs/guides/decisions.md::L37` | Kept for history; do not reopen |
| L10 | Storage backends are pluggable: `BlobStore` Protocol + `Queue` Protocol; selected via env vars. | `CLAUDE.md` §"Locked Tech Decisions" | Real swap points (FS / S3 / memory; ARQ / inline) for different deploys |
| L11 | Routes must declare `response_model`. SDK codegen falls back to `Any` otherwise. | `CLAUDE.md` §"Routes must declare `response_model`" | Typed SDKs depend on it; the intentionally-untyped binary/SSE/204 routes are the documented exception (count pinned by `test_no_regression_in_untyped_route_count`) |
| L12 | Generated SDKs (Python + TS) are canonical; hand-rolled SDKs deprecated as of 0.0.2, **removal milestone: 0.1.0** (every wire-shape change since L20 has cost dual maintenance). The deprecation warning at `../sfmapi-sdk/python/sfmapi_client/__init__.py` embeds the same target tag. | `CLAUDE.md` §"Hand-rolled SDKs are deprecated" + `docs/_internal/aip_audit_2026.md` (smells round, gap #8) | Codegen + ergonomics shim reaches feature parity; hand-rolled becomes dead weight |
| L13 | Job rollup via `_maybe_finalize_job` after every Task transition. Terminal vocabulary: `succeeded \| failed \| cancelled \| cancelled_dirty`. | `CLAUDE.md` §"Regression guards in `tests/contract/`" | `wait_for_job` and SSE-stream termination both depend on it |
| L14 | SSE stream closes after one drain cycle past terminal. | `CLAUDE.md` §"SSE stream termination" | Without this, `submit_and_stream` consumers block forever |
| L15 | `InMemoryBlobStore` is a process-local singleton. | `CLAUDE.md` §"`InMemoryBlobStore` is a process-local singleton" | Per-call construction broke multi-call flows because bytes live in per-instance dicts |
| L16 | Ephemeral mode (`SFMAPI_EPHEMERAL=true`) flips to in-memory SQLite + memory blob + inline queue + tempdir workspace. | `CLAUDE.md` §"Ephemeral mode" | Single-binary, zero-persistence demo / embedded path |
| L17 | One-shot feature endpoint at `POST /v1/oneshot/features`. Bytes-in / typed-result-out, no DB row, no Job, no cache. Tempfile is the only "persistence" and is deleted before the response. | `app/api/v1/oneshot.py` + `app/services/oneshot_service.py` + `docs/_internal/oneshot_streaming_proposal.md` | Single-call ergonomic for "give me SIFT keypoints from this image, right now" — replaces the eight-step resource-API flow |
| L18 | One-shot localize endpoint at `POST /v1/oneshot/localize?recon_id=...`. Bytes-in / typed-result-out, no Image / Blob / Upload / Job row. Resolves recon → sparse_dir under tenant; query image hits a tempfile then is deleted. Result reuses `LocalizationResult` shape verbatim. | `app/api/v1/oneshot.py` + `app/services/oneshot_service.py::localize_oneshot` + `docs/_internal/oneshot_streaming_proposal.md` | Replaces the eight-step "upload + register + submit-localize + poll + decode" flow with one HTTP request — the canonical "static-map relocalization" consumer flow (AR overlay, heritage doc, fixed-environment SLAM) |
| L19 | RFC 7807 envelope for `RequestValidationError` (422) — `{type, title, status, detail (str), instance, errors[]}`. FastAPI's default Pydantic shape `{detail: [...]}` is non-conformant; the new handler wraps it. `detail` is a human summary, `errors[]` preserves the structured per-field Pydantic errors. Error `type` URIs use the canonical `https://sfmapi.github.io/errors/<slug>` GitHub Pages domain. | `app/main.py::request_validation_handler` + `app/core/errors.py::SfmApiError.as_problem` + `docs/_internal/aip_audit_2026.md` | Surfaced by AIP audit (Agent 3); fixes wire-shape inconsistency that broke SDK ergonomics' `raise_for_status` parsing |
| L20 | AIP-158 pagination: list endpoints use `page_token` / `page_size` query params and return `Page{items, next_page_token, total}`. SSE/WS event replay (`start_cursor` on `/v1/jobs/{id}/events`) is a distinct concept and stays as-is. | `app/schemas/api/common.py::Page` + `app/api/v1/{projects,datasets,images,reconstructions}.py` + `docs/_internal/aip_audit_2026.md` | Codegen helpers (openapi-fetch, openapi-python-client, Google generators) auto-detect AIP-158 envelopes; renaming unlocks free iterator-helper generation across all SDKs |
| L21 | Lifecycle status fields are typed `Literal[...]` on the wire (AIP-216): `JobStatus`, `TaskStatus`, `UploadState`, `ReconstructionStatus` declare their closed sets in `app/schemas/api/`. ORM stays `String(...)` — only the response model gets typed. | `app/schemas/api/jobs.py` + `app/schemas/api/uploads.py` + `app/schemas/api/reconstructions.py` + `docs/_internal/aip_audit_2026.md` | SDK codegen produces string-union types instead of `string`; consumer typos against terminal-state literals fail at compile time |
| L22 | `JobAcceptedResponse` carries every stage-specific key as a typed Optional field (`target_recon_id`, `source_recon_ids`, `strategy`, `applied_sim3`, `method`, `recon_id`, `dataset_id`, `project_id`); `extra="allow"` removed. Routes no longer dict-spread untyped keys onto the envelope. | `app/schemas/api/jobs.py::JobAcceptedResponse` + `app/api/v1/{reconstructions,similarity}.py` + `tests/contract/test_e_generated_ergonomics.py::test_job_accepted_response_carries_stage_specific_typed_fields` | SDK codegen surfaces every stage-specific key as a typed accessor; previously consumers got `Any`/`Record<string, unknown>` and lost autocomplete on the very fields the route promised |
| L23 | LRO custom verbs use AIP-136 colon syntax: `POST /v1/jobs/{id}:cancel`, `POST /v1/jobs/{id}:resume`, `POST /v1/uploads/{id}:finalize`, `POST /v1/datasets/{id}/images:batchCreate` (was `:batch`). `DELETE /v1/jobs/{id}` retired — cancellation is a side-effecting operation with `force` flag, not a deletion. Batch shape uses `BatchCreateImagesRequest{requests}` / `BatchCreateImagesResponse{images}` (AIP-231) instead of `ImageBatchCreate`/`Page[ImageOut]`. | `app/api/v1/{jobs,resume,uploads,images}.py` + `app/schemas/api/images.py` + SPEC.md §6.5/§6.7/§9.2/§9.3 + `docs/_internal/aip_audit_2026.md` | Custom-verb consistency across the API; codegen helpers detect AIP-136 colon verbs and AIP-231 batch shapes for typed dispatch |
| L24 | List endpoints implement real AIP-158 keyset pagination end-to-end (`projects`, `datasets`, `images`, `submodels`, `jobs`); `dict`-typed wire fields replaced with typed schemas (`JobAcceptedResponse.applied_sim3: Sim3`, `ReconstructionOut.spec: PipelineSpec`, `Image/PointObservationRow` for observations, `UploadEntrySpec` for `entries`); `SourceSpec` carries an explicit `Annotated[..., Field(discriminator="kind")]` wrapper so OpenAPI emits a proper tagged union. New `GET /v1/jobs?status=...` list endpoint with closed-set status filter. | `app/services/{dataset,reconstruction,job}_service.py` + `app/api/v1/{datasets,reconstructions,jobs}.py` + `app/schemas/api/{jobs,reconstructions,datasets}.py` + `docs/_internal/aip_audit_2026.md` | Closes the "schema commits to pagination but service ignores it" wire-correctness gap; SDK codegen produces typed accessors instead of `Record<string, unknown>` for stage results / discriminated unions |
| L25 | Documentation polish: every `@router.<method>` route has a meaningful docstring (~36 routes); discriminated-union schemas (`PipelineSpec`, `ProgressEvent`, `SourceSpec`) document the discriminator + variants + forward-compat rule + capability-flag mapping; resource models (`JobOut`/`TaskOut`/`ReconstructionOut`/`SubModelOut`/`DatasetOut`) carry rollup-semantics docstrings; every `app/core/errors.py` class documents HTTP status + when-to-raise + envelope extras. Auto-generated OpenAPI / SDK codegen now reflects the full contract. | `app/api/v1/*.py` + `app/core/errors.py` + `app/schemas/api/*.py` + `app/schemas/pipeline_spec.py` + `app/schemas/progress_event.py` | Operator/SDK-consumer ergonomics; Redoc / Swagger UI / generated client tooltips become useful instead of bare path strings |
| L26 | AIP-155 request correlation: `RequestIdMiddleware` (`app/main.py`) reads `X-Request-ID` (or mints a ULID), echoes the resolved value back in the response header, and binds it to `structlog` contextvars for the request lifetime. Header rides CORS via `expose_headers`. | `app/main.py::RequestIdMiddleware` + `app/core/logging.py::bind_request_context` | Lets clients stitch logs across the boundary without server-side per-handler glue; covers streaming (SSE / file) responses too |
| L27 | `Task` carries TWO state columns: `task_state_json` (pre-execution carrier of `inputs` + `spec`, populated by `materialize_dag`) and `outputs_ref_json` (post-execution result, written by the dispatcher; surfaced on the wire as `TaskOut.outputs_ref`). Worker handlers read state via `app/workers/_task_io.py::read_state`. The previous overload of `outputs_ref_json` for both purposes caused a 19-file `__inputs`/`__spec` magic-key duplication and made `TaskOut.outputs_ref` impossible to type cleanly (it was both inputs-on-the-way-in and result-on-the-way-out). Migration `0007_task_state_json` adds the new column; pre-release with no in-flight data so no backfill needed. | `app/db/models.py::Task` + `alembic/versions/0007_task_state_json.py` + `app/services/job_service.py::materialize_dag` + `app/workers/_task_io.py` + `app/workers/dispatcher.py` (writes results to `outputs_ref_json`) + `docs/_internal/aip_audit_2026.md` (smells round, gap #5) | Closes the highest-severity smell-audit finding; ships proposal P1's direction; unlocks future typed `outputs_ref` discrimination |
| L28 | Singleton-aware `BlobStore` factory: backends declare `is_singleton: bool` (instance-state lifetime) and `get_blob_store()` caches singletons in a `_INSTANCES: dict[type, BlobStore]` map keyed by class. Replaces the previous `if cls is InMemoryBlobStore` class-conditional dispatch with a uniform flag check; future singleton backends (e.g. ConnectionPool-backed S3) opt in by overriding the flag. | `app/storage/blobs.py::{FSBlobStore,S3BlobStore,InMemoryBlobStore}.is_singleton` + `app/storage/blobs.py::get_blob_store` + `docs/_internal/aip_audit_2026.md` (smells round, gap #6) | Eliminates a class-conditional-dispatch design smell; preserves `L15`'s singleton invariant uniformly; tested by the existing L15 regression guard |
| L29 | ~~`ColmapModBackend` aggregator (1004-line god class) deferred as an intentional aggregator.~~ **Superseded by L31** — the backend was extracted out of the repo entirely; this concern is moot. The structural guidance (composition split when methods exceed 30) still applies to whatever backend package eventually inherits it. | `docs/guides/decisions.md::L31` | Kept for history; do not reopen |
| L30 | Job-submitting service helpers (`submit_features`, `submit_matches`, …) all route through `_submit_single_stage()` in `app/services/sfm_stage_service.py`. The helper wraps the common `[_stage_node(...)] + submit_job_dag(...)` tail so cross-cutting changes (priority, request-id propagation, capability auto-check) land in one place instead of 13. Reconstruction paths share a `_reconstruction_paths(tenant_id, r) -> (rec_root, sparse_dir)` helper. Web-layer 202+Location envelope construction lives in `app/api/v1/_helpers.py::accepted_response` (NOT in the schemas layer — schemas remain Pydantic-only). | `app/services/sfm_stage_service.py::_submit_single_stage` + `app/api/v1/_helpers.py::accepted_response` + `docs/_internal/aip_audit_2026.md` (smells round 2) | Closes round-2 god-service finding + layering-inversion finding from the post-closure audit |
| L31 | sfmapi ships **no concrete SfM backend**. The `SfmBackend` Protocol (`app/adapters/backend.py`), the registry (`app/adapters/registry.py`), and a no-op `StubBackend` (`app/adapters/stub_backend.py`) are the entire `app/adapters/` surface; concrete engine implementations (pycolmap, OpenSfM, hloc, custom forks) live in **separate packages** and call `register_backend("name", Backend)` at startup. The registry has no implicit default — `get_backend()` raises `KeyError` when nothing is registered. Ephemeral mode (`SFMAPI_EPHEMERAL=true`) auto-registers the stub for demos / smoke tests. The flat `colmap_sha`/`baxx_sha`/`cudss_ver`/`cuda_arch`/`sam_model_sha` `RuntimeVersion` columns collapse to a single backend-defined `runtime_version_id` opaque string; migration `0008_runtime_version_collapse` is destructive (pre-release). `/v1/version` returns `{sfmapi, backend?}` with `backend = {name, version, vendor, runtime_versions}` populated from the registered backend's `runtime_versions()`. | `app/adapters/{backend,registry,stub_backend}.py` + `alembic/versions/0008_runtime_version_collapse.py` + `app/api/v1/health.py` + `app/schemas/api/common.py::{BackendVersion,VersionResponse}` + `docs/guides/backend_implementations.md` | Supersedes the colmap_mod-bundled framing; makes sfmapi a real wire standard rather than a single-engine wrapper. **Replaces L29** — the `ColmapModBackend` god-class concern is moot now that the file lives outside this repo |
| L32 | Standalone is the default deployment shape. `.env.example` ships with `SFMAPI_QUEUE_BACKEND=inline`, `SFMAPI_BLOB_BACKEND=fs`, `SFMAPI_DB_URL=sqlite+aiosqlite:///./sfmapi.db`. `clone -> uv pip install -> alembic upgrade -> uvicorn` boots the app without Docker, Redis, or Postgres. Multi-instance / GPU-fleet deploys flip `SFMAPI_QUEUE_BACKEND=arq` (consumes the optional `SFMAPI_REDIS_URL`) and point `SFMAPI_DB_URL` at Postgres. The previous default (`queue_backend=arq`) made first-run require a Redis — friction with no payoff for the standalone case sfmapi specifically targets. | `.env.example` + `README.md` (Quickstart) + `docs/guides/quickstart.md` + `app/core/config.py::Settings` defaults | Lowers first-run friction to zero; aligns the docs with the codebase's actual shape (the inline queue and FS blob store have always been first-class, the env defaults just didn't reflect that) |

| L33 | AIP-161 `update_mask` is supported on PATCH for projects and datasets while preserving the legacy implicit mask when omitted. Images now support canonical ID deletion via `DELETE /v1/images/{image_id}`; the label-addressed `DELETE /v1/datasets/{dataset_id}/images/{name}` path stays as a compatibility alias. | `app/api/v1/{projects,datasets,images}.py` + `app/api/v1/_helpers.py::masked_updates` + `SFMAPI-SPEC.md` sections 6.2/6.4/6.5 | Adds AIP-134/AIP-161 and AIP-122 affordances without breaking existing clients or recorded fixtures |

| L34 | Backend integration is layered. The minimum registered backend satisfies `Backend` (`name`, `version`, `vendor`, `capabilities()`, `runtime_versions()`); action-only packages expose native tools through `/v1/backend/actions`; portable stage packages implement only the narrow protocol surfaces they support; `SfmBackend` remains the full union for complete engines. Workers call `require_backend_method(...)` before optional stage dispatch so missing methods return the normal 501 capability error. | `app/adapters/backend.py` + `app/workers/tasks/*.py` + `docs/guides/backend_implementations.md` | Lets vendor CLIs, Python research repos, C++ bindings, and full in-process engines share one discovery/job/progress shell without fake placeholder methods or 500s from `AttributeError` |

| L35 | Provider-aware worker routing. Worker handlers resolve their backend per task by reading `spec.provider` (via `app/workers/backend_resolver.py`); `app.adapters.registry.register_backend(name, factory, providers=[...])` aliases the same factory under sfm_hub provider ids; `sfm_hub.discovery.load_backend_entry_points(register_provider=...)` registers manifest-declared aliases on lifespan boot. Combined pair-selection + matching tasks reject mismatched `pairs.provider` / `matcher.provider` because the worker runs them through one `backend.match(...)` call. The `provider` selector is exposed on every job-submitting surface (SfM stages, recipes, utility stages, one-shot, artifact convert, backend actions, MCP discovery). | `app/adapters/registry.py` + `app/workers/backend_resolver.py` + `sfm_hub/{discovery,routing}.py` + `app/services/{sfm_stage,backend_action,artifact_conversion}_service.py` + `docs/guides/backend_implementations.md` §"Provider routing" + SPEC §6.6/§6.10 | Multi-backend deployments need per-call routing without flipping `SFMAPI_BACKEND` for the whole process. The legacy single-backend fallback is preserved when no provider is set, so single-backend installs keep working unchanged |
| L36 | Capability vocabulary extension (round-2 plugin completeness audit). Nine capability strings added to `OPTIONAL_CAPABILITIES`: `features.extract.sosnet`, `geometry.two_view`, `ba.rig`, `index.vocab_tree`, `georegister.gps`, `image.undistort` (in scope per explicit decision — a portable sparse-SfM post-process, NOT dense MVS), `rigs.configure`, `pose_priors.mapping`, `compute.in_memory`. New `@runtime_checkable` Protocols: `GeometryBackend`, `VocabTreeBackend`, `UndistortBackend`, `RigBackend`, plus `TransformBackend.align_reconstruction`. `pose_priors.read_write` stays sfmapi-internal (accept/store/forward); the new `pose_priors.mapping` is the backend-driven flag for "the backend consumes priors in mapping" — closes the discoverability trap where a client could not tell whether forwarded priors were used. Dense MVS / meshing remain out of scope (Appendix D) — reachable only via the backend-action catalog; stale `dense.*` strings purged from SPEC examples. Radiance / 3DGS now has portable `radiance.train`, `radiance.evaluate`, and `radiance.metrics.*` capabilities with `/v1/radiance_fields/...` train/evaluate resource routes; render/export/convert stay backend actions or future Processor proof cases until they have portable routes. The processor-framework plan keeps radiance as the plugin Data Type / Processor proof case rather than forcing it into sparse-core stages. | `app/core/capabilities.py` + `app/adapters/backend.py` + `app/adapters/stub_backend.py` + `sfmapi/backends.py` + `SFMAPI-SPEC.md` §3.11 + Appendix D | A round-2 audit across the 7 reference plugins found real native features the engines support but no capability string could express. Radiance now has portable job-submission routes. For the remaining non-radiance sparse post-process stages, adding the vocabulary makes them discoverable/declarable while full portable job-submission routes remain follow-up; until then the capability flag plus the backend-action catalog give an honest discover + invoke path |
| L37 | WebSocket job feed ships **alongside** SSE (supersedes L9). `/ws/v1/jobs/{job_id}` streams the same `ProgressEvent` vocabulary and accepts `cancel` / `ping` client ops; `?last_event_id=N` resume mirrors the SSE ring buffer; a plain HTTP GET on the path returns a `ws_endpoint` discovery hint. **SSE remains the primary progress feed** — WS exists for browser viewers that want bidirectional cancel + peek. The WS surface is intentionally outside the OpenAPI document (codegen cannot express WebSocket); its contract is `SFMAPI-SPEC.md` §8 plus the server tests. Recorded during lean-audit remediation (6.1); the code predates this row. | `app/api/v1/ws_jobs.py` + `app/main.py` (router include) + `SFMAPI-SPEC.md` §8 + `tests/e2e/test_ws_reserved.py` | One-directional SSE stays sufficient for progress; WS adds in-band cancel for browsers without growing the generated-SDK surface |
| L38 | Services↔adapters layering rule amended (lean audit 3.5): `app/services` MAY import the adapters **public contract layer** — `backend`, `registry`, `backend_config`, `backend_actions`, `backend_artifacts` — public names only. Every other `app.adapters` module (stub backend, image adapter, ...) and any `_private` symbol stays off-limits from services. Replaces the old absolute "services never import adapters" rule, which was broken 6× because services legitimately need backend discovery/dispatch; `radiance_train_option_schema` went public as part of the same change. Enforced by an AST guard. Shipped in `ea76452`. | `tests/unit/test_repo_boundary_guards.py::test_services_import_only_public_adapter_contract_surface` + `app/adapters/backend_config.py` + `CLAUDE.md` §"Imports" | An enforceable narrow rule beats an aspirational absolute one; the AST guard turns future drift into a CI failure instead of silent accumulation |
| L39 | Dependency readiness is single-sourced: `app/orchestrator/readiness.py` owns `READY_DEPENDENCY_STATUSES` (`{succeeded, skipped}` = ready) and the readiness predicates shared by the scheduler, the dispatcher, and the janitor. Fixes a real bug — the scheduler counted only `succeeded`, so a task depending on a cache-`skipped` upstream was never enqueued at submit time and waited for a janitor sweep. Shipped in `95a89af`. | `app/orchestrator/readiness.py` + `tests/unit/test_readiness.py` + `tests/integration/test_scheduler_readiness.py` | Three drifting copies of one vocabulary caused a scheduling stall; one module makes the vocabulary drift-proof |
| L40 | `sfmapi.plugin_service` is the **only supported surface for container-service plugins** (protocol `sfmapi-plugin-http-v1`, `PROTOCOL_VERSION` 1.1): `build_plugin_server(...)`, `ManifestBackend`, `TaskExecutor`, and the protocol helpers, re-exported from `app/plugin_server.py`. Hand-rolled plugin `server.py`/`protocol.py` pairs are retired — the 5 radiance plugin repos (brush, gsplat, fastergs, lfs, spirulae) adopted the kit; vismatch deferred because its `container_service` speaks the backend-action protocol, not the radiance execute protocol. Shipped in `48f19c5` (core) + per-plugin-repo commits. | `sfmapi/plugin_service.py` + `app/plugin_server.py` + the radiance plugin repos' `server.py` | Plugins only adopt public `sfmapi.*` surfaces — publishing the kit took adoption from zero to five repos and deleted five drifting hand-rolled protocol copies |
| L41 | v0 kernel fenced behind a preview conformance level (lean-audit gate D1, decided 2026-07-16). The default contract is the kernel — projects → uploads → datasets → images → SfM stages → jobs/events → reconstructions → artifacts + backend discovery — with radiance as a labeled extension. **Admin routing profiles (5 of the 16 admin ops), dataflow/processors (6 ops), and similarity are preview surfaces**: settings-driven fencing (`include_in_schema`) drops them from the default OpenAPI document; conformance tests assert the split. Target ≈ 100 ops in the default contract (from 136). Implementation = lean-audit item 7.1, landing 2026-07-16. | `docs/guides/lean_audit_2026.md` §Phase 0 D1 + §7.1 | The 0.0.1 surface (123 paths / 136 ops / 215 schemas) grew ahead of any consumer; fencing the unproven surfaces as preview shrinks the contract that SDK codegen and conformance must chase, without deleting working code — the spec already defines conformance levels, so the mechanism is native rather than bolted on |
| L42 | `sfmapi-cpp` frozen/archived in place (lean-audit gate D2, decided 2026-07-16). The sibling `../sfmapi-cpp` repo carries a prominent README STATUS banner; its parity workflow is reduced to `workflow_dispatch`-only (push / pull_request / nightly schedule triggers removed; nothing deleted, so re-enabling = restoring the triggers). **The Python server is the reference implementation** of the wire spec. Revisit criterion: **wire-spec stability** — the Python surface stops moving (kernel fencing L41 and custom-verb normalization 6.3 landed, a stable contract tag exists). Executed as item 7.3; shipped in `sfmapi-cpp@9aedf30`. | `docs/guides/lean_audit_2026.md` §Phase 0 D2 + §7.3 + `../sfmapi-cpp/README.md` + `../sfmapi-cpp/.github/workflows/parity.yml` | The 25.8k-line parity harness re-verified a moving target — every wire change cost a second server implementation plus harness churn — and no other repo references the C++ port; freezing (not deleting) keeps the port and harness ready for when there is a stable spec worth verifying against |
| L43 | Plugin-family repo consolidation (lean-audit gate D3, decided 2026-07-16): the 5 radiance plugin repos (brush, gsplat, fastergs, lfs, spirulae) merge into one `sfmapi_radiance` with per-provider extras (item 4.3 — **merge executing now**), and the 3 COLMAP repos merge into one `sfmapi_colmap` with three providers via the documented `Plugin.register_hook` multi-backend pattern (item 4.4 — **approved + scheduled**). 17 → 11 repos, no monorepo migration. **Executed — all family merges landed (see L46):** the radiance five merged as the 3DGS family (now dist `sceneapi-3dgs`); the COLMAP trio merged and then absorbed instantsfm + spheresfm + realityscan into the SceneMap family (`sceneapi-map`); vismatch + hloc merged into the SceneMatch family (`sceneapi-match`) under the P7 org layout. | `docs/guides/lean_audit_2026.md` §Phase 0 D3 + §4.3/§4.4 | The families already behave like one codebase split across repos: the COLMAP trio ships a byte-identical `model.py` ×3 (md5-equal — pure repo tax) while its `pycolmap_backend.py` **forked and drifted 1,032 lines** across two repos (divergence where none was intended); the radiance five each copy the 1,143-line multi-provider `trainer.py` (brush↔lfs differ by 10 lines; ~3,400 duplicated lines total). Same-family repos duplicate what they should share and fork what they should not — one repo per family removes both failure modes |
| L44 | Top-level `app` package folds under the `sfmapi` namespace (lean-audit gate D4, decided 2026-07-16): **approved + scheduled** as its own focused mechanical change (item 7.4) that must land **before any external release**; a temporary top-level `app` shim module ships for exactly one release, then is removed. **Executed:** the fold shipped in `c815aee`; the `app` shim was removed in 0.1.0 as scheduled (the same release renames the package to `sceneapi`, with a one-release `sfmapi` alias shim — see `CHANGELOG.md` 0.1.0 and `docs/_internal/sceneapi_migration_proposal.md` Phase B). | `docs/guides/lean_audit_2026.md` §Phase 0 D4 + §7.4 + `pyproject.toml` (`packages` list) | The wheel currently owns the global `app` name — a permanent import-collision hazard for every consumer environment — and the private `app.*` tree is the root cause of good shared code being invisible to plugins (the very failure L40 had to patch by re-exporting `app/plugin_server.py` as `sfmapi.plugin_service`). Folding before an external consumer exists makes the rename cheap exactly once |
| L45 | Package identity renamed `sfmapi` → `sceneapi` (migration P7 phase W7, gate G2, executed 2026-07-17): distribution `sceneapi` 0.1.0, import package `sceneapi` (internal tree `sceneapi.server`), CLI `sceneapi` / `sceneapi-mcp`, env prefix `SCENEAPI_*`, entry-point group `sceneapi.backends`. One-release deprecation shims (all removed in 0.2.0): `sfmapi` import alias package, `sfmapi`/`sfmapi-mcp` alias console scripts, `SFMAPI_*` env alias with construction-time DeprecationWarning, and the loader reading the legacy `sfmapi.backends` entry-point group. The 0.1.0 release bundles the already-scheduled breaks: hand-rolled SDK removal (L12) and `app` shim removal (L44). Wire identity is untouched until Phase C (G3): SFMAPI-SPEC.md, `sfmapi.*.v1` format ids, `x-sfm-*` media types, sfmapi.github.io problem URIs, `sfmapi-plugin-http-v1`. Shipped in `6321b2b` (+ `aefaf4c` SDK-ref follow-up). | `docs/_internal/sceneapi_migration_proposal.md` §Phase B + locked spec + `CHANGELOG.md` 0.1.0 + `tests/unit/test_rename_compat.py` | Identity changes are cheap exactly once — before external consumers exist (the 3dgsapi → sfmapi rename and the L44 `app` fold both proved the staged-shim pattern); landing it at 0.1.0 rides the release that already carried every planned break |
| L46 | Plugin/SDK family repo layout executed under the SceneAPI org names (migration P7 phase W8, executed 2026-07-17, completes L43): **SceneMatch** = vismatch + hloc merged as import `sceneapi_match`, dist `sceneapi-match` (`SceneMatch@83d2de7`); **SceneMap** = COLMAP {native, pycolmap, cli} + instantsfm + spheresfm + realityscan merged as import `sceneapi_map`, dist `sceneapi-map` (`SceneMap@05c1817` + suite port `e155968`); **3DGS** = `sfmapi_radiance` renamed to import `sceneapi_3dgs`, dist `sceneapi-3dgs` (`sfmapi_radiance@44ed1e0`); **bench** renamed dist `sceneapi-bench` (`sfmapi-bench@4612829`); **SDK repo** renamed the generated Python SDK to import `sceneapi_client_gen`, dist `sceneapi-client` (`sfmapi-sdk@63998fe`) and the TS package to `@sceneapi/client` (`sfmapi-sdk@db8b0b2`), and the deprecated hand-rolled Python SDK + its CLI were **removed at 0.1.0 as scheduled** per L12 (`sfmapi-sdk@2f1d8b0`). Provider ids, entry-point names, and console-script names are unchanged; plugin dists all 0.1.0. | `docs/_internal/sceneapi_migration_proposal.md` §locked naming scheme + the cited repo commits + `sfm_hub/registry/backends/*/manifest.json` (W9 coordinate sweep) | The merges land against the renamed core in one pass so every plugin coordinate flip (package, import path, repo URL) happens once, before the org push publishes any of them |
| L47 | Contract plane shipped (proposal P8 steps 0-9, executed 2026-07-18). `sceneapi-io` 0.2.0 carries the data + procedure contracts (`data`/`formats`/`mapping`/`matching` namespaces, numpy-native, import-isolated, guard-tested); core re-homes its DataType/format vocabulary onto it **byte-identically** and gains a distinct `map_feed_forward` Processor + `feed_forward` recipe + `map.feed_forward`/`match.detector_free`/`keypoints.persistent` capabilities + io dual-dispatch, so a representation-neutral `Mapper` (correspondences optional, per-pixel confidence, `SE3\|None` unregistered views) routes through the unchanged orchestrator/wire/job machinery. **SceneMatch 0.2.0** (`8c1a74b`) vismatch/hloc conform the matching contracts natively; **SceneMap 0.2.0/0.3.0** (`5c7d6d2`) ships the `ColmapMapper` conformer and the **MapAnything** feed-forward provider (Apache-safe weights default, torch-free import); a feed-forward reconstruction trains 3DGS via the existing recon path, proven e2e. `colmap_actions` deliberately NOT evicted (cross-tier C++-parity contract). Core suite 1210 passed. Step 10 (refinement/localization/retrieval contracts, 3DGS namespace, Phase-C wire exposure) deferred as future per-item decisions. | `docs/_internal/contract_plane_proposal.md` execution log + core `a7ab342`/`f81c7ef`/`2c1547d`/`c5a7f4b`/`1db316b`/`d20b36d` + `SceneIO@704e6f4` | The core mapping Protocol was a COLMAP adapter signature (`db_path`/`sparse_root`/closed kind vocab) a feed-forward model could not honestly implement; a representation-neutral contract in a leaf package lets classical and learned mappers share one orchestrator, wire, and job machinery with zero routing changes — proven by MapAnything plugging in with no core edits |
| L48 | **Family de-prefix** (P9, executed 2026-07-20): the `sceneapi-*` satellite packages are renamed to bare names matching their repos — `sceneapi-io`→`sceneio`, `sceneapi-map`→`scenemap`, `sceneapi-match`→`scenematch`, `sceneapi-3dgs`→ dist `3dgs` / import `gs3` (import identifiers cannot start with a digit), `sceneapi-bench`→`scenebench`, `sceneapi-client`→`scenesdk` (TS `@sceneapi/client`→`@scenesdk/client`); import packages follow (`sceneapi_io`→`sceneio`, `sceneapi_map`→`scenemap`, …). Reverses the "keep sceneapi-*" stance of L46. Core stays `sceneapi` (unchanged, already published 0.1.0) — it only re-points its `sceneio` dep + imports; the entry-point group `sceneapi.backends` is unchanged (only entry-point values move). **No deprecation shims**: nothing but core was published, and every target PyPI name is the user's own reserved 0.0.1 placeholder (SceneAPI-owned), so releases publish over them. Local suites green across all seven repos (core 1210, SceneIO 356, SceneMap 395, SceneMatch 68, 3DGS 59, SceneBench 34, SDK import + core contract 90). | the seven repos' P9 commits + `sfm_hub/registry/backends/*/manifest.json` + `docs/_internal/sceneapi_migration_proposal.md` §P9 | `sceneio` (not `sceneapi-io`) drops "api" from a pure data-contract package; aligning each package with its repo name pre-publish is the cheapest identity change (same "cheap exactly once" logic as L45) and finally uses the reserved names that sat idle since the org scaffold |

## Cancelled with rationale

These were considered, evaluated, and rejected. The rationale lives
here so the conversation doesn't restart every six months.

| ID | Item | Cancelled because |
|---|---|---|
| C1 | Unify S3 caches (`S3Cache` + `S3BlobStore._blob_cache`) | Two caches solve different problems: `S3Cache` is mutable-key etag-LRU for `S3Source` images; `S3BlobStore` cache is content-addressed by sha. Forcing them to share storage would be wrong; the actual code duplication is ~10 LOC of "atomic write + rename". |
| C2 | Settings into composed submodels | Mostly cosmetic given env-var loader has to stay flat (`SFMAPI_X` not `SFMAPI_X__Y`). Section comments already group fields. Real submodel composition without changing env-var paths would be decoration. |
| C3 | C++ live-server contract test | C++ ships no built-in HTTP transport (consumers BYO libcurl / cpp-httplib / Emscripten Fetch). `windows.h` macro pollution + `CreateProcess` env-var quirks + process-orphan cleanup all bit during the prototype. Coverage equivalence preserved through `test_contract.cpp` + Recorder transports. A C++ live test would only prove "the bound transport works" — which is the consumer's transport, not the SDK's. |
| C4 | Unify the three resume primitives into one `Checkpoint` type | After mapping `MappingInput` (worker mid-task state) + sealed snapshot (publishable end-of-stage view) + `Task.cache_key` (content-addressed task-result lookup), they solve genuinely different problems. A unified type would be wider than the intersection of usefulness. See `docs/_internal/resume_unification_proposal.md` for the analysis; the three small concrete actions (rename for clarity, add `resume_inventory` helper, document the split) are recommended but the unification itself is rejected. |
| C5 | "Multipart-as-snapshot" S3 sealed-snapshot protocol | Bundling a snapshot's files into one tar/zip kills sparse reads, kills cross-snapshot dedup, kills HTTP caching. Misuse of multipart. Rejected in favor of the manifest-pointer approach. See `docs/_internal/sealed_snapshots_on_s3_proposal.md` Option B. |
| C6 | "Copy-then-delete" S3 sealed-snapshot protocol | Not atomic, doubles every write, wastes the dedup property. All disadvantages of the manifest-pointer approach with none of the advantages. See same proposal Option C. |

## Proposed — waiting on user input

Each of these has a long-form design doc on disk. Status flips to
"approved" once the user signs off (and to "locked" once shipped).

| ID | Proposal | Status | Source | Blocked on |
|---|---|---|---|---|
| P1 | Resume primitive **non**-unification: rename `MappingInput`-checkpoint → `mapping_state`, add `resume_inventory(job_id, recon_id)` helper, document the three-way split. | Ready to ship; ~2h work. | `docs/_internal/resume_unification_proposal.md` | Single user `OK` |
| P2 | Sealed snapshots on object storage via manifest-pointer + content-addressed blobs. | Ready to ship Phase a only (~6h); phases b–d follow. | `docs/_internal/sealed_snapshots_on_s3_proposal.md` | Single user `OK` |
| P3 | Postgres RLS for tenancy + 3-layer defense (app `WHERE` + DB policy + conformance audit). | Ready to ship if Postgres-only is committed (~6h for phases a+b). | `docs/_internal/rls_postgres_tenancy_proposal.md` | **Postgres-only commit** (drops L6's dual-dialect rule) |
| P4 | One-shot streaming endpoints under `/v1/oneshot/...`. Phase a (`oneshot/features`) **shipped as L17**. Phase b (`oneshot/localize`) **shipped as L18**. Phase c (`oneshot/match`) deferred until a real consumer asks. | Phases a + b shipped. | `docs/_internal/oneshot_streaming_proposal.md` | Phase c: defer indefinitely |
| P5 | Streaming SLAM endpoint at `/ws/v1/slam/sessions/{sid}/frames` with `Session` resource + `SlamBackend` Protocol + OpenVINS reference adapter. WebSocket bidirectional framing, frames in / poses out at ~30 Hz, sparse persistence (keyframes + on-demand map snapshots only). | Phases a-d ready to design; ~40-45h total. **Touches L4 (live map reads)** — would unlock it. (L9 no longer gates this: the WebSocket transport shipped, see L37; the SLAM surface itself is the remaining question.) | `docs/_internal/streaming_slam_proposal.md` | **Confirmed consumer with VIO data** + readiness to unlock L4. Otherwise: track as future architectural reference; recommend `slamapi` sibling service when needed. |
| P6 | In-memory reconstruction handle for `compute.in_memory` backends. Analysis concludes a non-path `ReconstructionHandle` Protocol is **premature**: fork-per-task + one-Task-one-ARQ-job (L5) + sealed-snapshot-only reads (L4) bound any in-memory handle to within-Task scope, where it is an implementation detail of a not-yet-existing fused-recipe Task kind rather than a wire contract. Recommendation: document `compute.in_memory` as an advisory scheduler hint, keep `MappingInput` as the sole cross-Task serialization format, defer the handle Protocol. | Recommend approving the doc clarification (~30 min); defer the Protocol. | `docs/_internal/in_memory_reconstruction_proposal.md` | Single user `OK` on the doc clarification; the deferred handle Protocol is blocked on a fused-recipe Task kind existing + profiling as I/O-bound. |
| P7 | Migrate the whole family to the `SceneAPI` GitHub org (staged: A hosting move -> B package rename `sfmapi`->`sceneapi` at 0.1.0 -> C wire identity -> D SFMAPI decommission). Gates ANSWERED 2026-07-17: G1 all scaffold names, G2 rename immediately (ships as 0.1.0), G3 wire waits for Phase C, G4 execute now + reserve PyPI. Execution: **W7 (core package rename, L45) and W8 (family merges/renames, L46) are done**; W9 (coordinate sweep to `SceneAPI/<repo>` URLs — bundled registry, CI checkout refs, deploy/docs org links — then org pushes, SFMAPI "moved" notes, PyPI reservation) in flight, sweep landed, pushes pending. Wire identity (Phase C) and SFMAPI decommission (Phase D) remain. |  | `docs/_internal/sceneapi_migration_proposal.md` **Org pushes executed 2026-07-17:** all seven SceneAPI-org repos live (SceneAPI@51b162d, SceneSDK, 3DGS, SceneMap, SceneMatch, SceneBench, sfmapi-cpp pushed+archived); all eleven SFMAPI-org repos carry moved-note banners (the eight absorbed plugin repos with their full final histories). Open: PyPI reservation (owner credentials), v0.1.0 tag push, Phase C wire identity, Phase D archival. | Gates G1-G4 |
| P8 | SceneIO becomes THE contract plane (gates locked 2026-07-17): `sceneapi_io.data/.formats/.mapping/.matching` import-isolated namespaces hold the data + procedure contracts; numpy becomes a hard dep (leaf property = imports nothing from the family, not stdlib purity); SceneMap/SceneMatch/3DGS remain conforming implementation bundles. Amends the one-contract-per-repo phrasing on verified economics (per-repo contract dists rated fatal-as-written twice: 7-dist 4-deep release trains, core pip-depending into plugin repos); extraction per family stays an option behind the namespace isolation guard. Mapping contract is representation-neutral (correspondences optional, per-pixel confidence, scale semantics) so MapAnything-class feed-forward mappers and COLMAP both conform. | **Steps 0-9 shipped 2026-07-18 (see L47)**; Step 10 (refinement/localization/retrieval contracts, 3DGS contract namespace, Phase-C wire exposure) deferred as future per-item decisions; compat-CI lane in flight. | `docs/_internal/contract_plane_proposal.md` | Steps 4/8/9 carry their own review points (core release, weights licensing, dense-to-3DGS bridge) |

## Decision flow

When a new architectural question comes up:

1. **Does it overlap a Locked decision?** → reopen requires
   explicit "L# is being unlocked" entry here, plus a written
   reason that updates the Source column.
2. **Does it overlap a Cancelled item?** → check the rationale.
   New evidence is required to reopen; "I think we should look at
   this again" without new evidence isn't.
3. **Does it overlap a Proposal?** → ping the user for the
   blocking decision; don't make the call solo.
4. **None of the above** → write a short proposal under
   `docs/_internal/<topic>_proposal.md` following the existing
   structure: problem, options, recommendation, cost, what-it-
   doesn't-do. Add a row to the Proposed table here.

## Counts (snapshot)

- Locked: 48 (L1–L48; L9 and L29 kept struck-through as superseded
  history; L37–L40 record decisions shipped in the 2026-07 lean-audit
  remediation; L41–L44 record the lean-audit Phase 0 gates D1–D4,
  decided 2026-07-16; L45–L46 record the P7 migration phases W7–W8
  executed 2026-07-17; L47 the P8 contract plane, L48 the P9 family
  de-prefix, executed 2026-07-18/20)
- Cancelled: 6
- Proposed: 8 (P8 contract plane steps 0-9 shipped as L47, step 10 deferred; P7 SceneAPI migration W7-W8 executed as L45/L46, W9 in flight; P4 phases a+b shipped as L17/L18; P1's direction shipped as L27; P2-P3 + P5-P6 still proposed)
- **Total tracked decisions: 60**

Counts here exclude per-feature implementation decisions (route
shapes, schema fields, SDK method names) — those live in their
own docs and tests. This register tracks decisions that span
multiple files / subsystems and where reverting would be expensive.
